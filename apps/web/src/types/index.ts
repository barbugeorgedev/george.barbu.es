export interface SEOProps {
  title?: string;
  description?: string;
  keywords?: string;
  url?: string;
  image?: string;
}

export interface EventParams {
  action: string;
  category: string;
  label: string;
  value: number;
}
