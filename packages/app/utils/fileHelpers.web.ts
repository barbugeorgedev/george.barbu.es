export const downloadFile = async () => {
  console.warn("downloadFile is not available on web.");
  return null;
};

export const saveBase64File = async () => {
  console.warn("saveBase64File is not available on web.");
  return null;
};
